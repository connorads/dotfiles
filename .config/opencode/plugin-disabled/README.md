Disabled OpenCode plugins live here.

OpenCode auto-loads any plugin in `~/.config/opencode/plugin/` and there is no
config toggle to disable a local plugin. Keep unused plugins in this folder so
they stay in source control without being loaded.

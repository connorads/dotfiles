minimumReleaseAge: 5760     # 4 days (minutes); explicit ⇒ strict enforcement
trustPolicy: no-downgrade
ignoreScripts: true
blockExoticSubdeps: true    # default true in v11; explicit ⇒ auditable + drift-proof

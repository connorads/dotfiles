# Notes on comparison

Prefer `a < b` to the older spelling.

Everything in this paragraph sits between a stray angle bracket and a later
one, so a tag stripper aimed at plain Markdown eats the whole thing. The
sentence that matters is this: a pointer is only evidence when the fetch and
the parse are both honest.

Note also that x > y holds throughout.

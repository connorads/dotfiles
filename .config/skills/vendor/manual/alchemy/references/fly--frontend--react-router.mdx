---
title: React Router
description: Deploy React Router v7 (framework mode) to Fly with Fly.Website.ReactRouter — SSR on a Machine, client assets baked into the image, and React Router's own Vite dev server under alchemy dev.
sidebar:
  order: 7
---

import { Code, Tabs, TabItem } from "@astrojs/starlight/components";

export const devDeps = "@alchemy.run/frontend-frameworks";

`Fly.Website.ReactRouter` deploys a
[React Router](https://reactrouter.com) v7 app in framework mode to Fly.
React Router builds through Vite, so Alchemy runs your project's own
build: the SSR server runs as a Node [`Fly.Service`](/fly/compute/services)
on a Machine, and client assets are baked into the image and served
first. Omit `app` and Alchemy creates a
[`Fly.App`](/fly/compute/apps) plus a shared IPv4 so
`https://{app}.fly.dev` answers. No adapter, no `fly.toml`.

## Install

The build integration is not bundled with alchemy. Install
`@alchemy.run/frontend-frameworks`; the resource loads its
`/react-router` and `/react-router/node` exports from your project at
deploy time. It is only used at build time, so a dev dependency is
enough:

<Tabs syncKey="pkgManager">
  <TabItem label="bun" icon="bun">
    <Code code={`bun add -d ${devDeps}`} lang="sh" />
  </TabItem>
  <TabItem label="npm" icon="npm">
    <Code code={`npm install -D ${devDeps}`} lang="sh" />
  </TabItem>
  <TabItem label="pnpm" icon="pnpm">
    <Code code={`pnpm add -D ${devDeps}`} lang="sh" />
  </TabItem>
  <TabItem label="yarn" icon="seti:yarn">
    <Code code={`yarn add -D ${devDeps}`} lang="sh" />
  </TabItem>
</Tabs>

The React Router side is whatever `npx create-react-router@latest`
scaffolds — `react-router`, `@react-router/dev`, `@react-router/node`,
`isbot`, and `vite`. Keep `@react-router/node` and `isbot` in
`dependencies`: React Router picks its default `entry.server` by reading
them from your `package.json`, and installs `isbot` on the fly if it is
missing.

## Configure Vite

Your `vite.config.ts` stays what the React Router scaffold gives
you — no adapter, no server preset. Vite plugins like Tailwind go in
`plugins` as usual:

```typescript
// vite.config.ts
import { reactRouter } from "@react-router/dev/vite";
import tailwindcss from "@tailwindcss/vite";
import { defineConfig } from "vite";

export default defineConfig({
  plugins: [tailwindcss(), reactRouter()],
});
```

`reactRouter()` owns routing, typegen, and both build environments.
Alchemy drives the same two passes `react-router build` runs, wraps the
server manifest with `createRequestHandler`, and packages the resulting
fetch handler as a Node HTTP server on port 3000.

:::caution[Remove @cloudflare/vite-plugin if present]
The React Router CLI adds `@cloudflare/vite-plugin` when you pick the
Cloudflare template. It builds for workerd, not Node, so a Fly deploy
needs it gone:

```diff lang="typescript"
// vite.config.ts
-import { cloudflare } from "@cloudflare/vite-plugin";
import { reactRouter } from "@react-router/dev/vite";
import { defineConfig } from "vite";

export default defineConfig({
-  plugins: [cloudflare({ viteEnvironment: { name: "ssr" } }), reactRouter()],
+  plugins: [reactRouter()],
});
```

:::

## Declare the Website

Declare the site as a module-level const (rather than inline in the
Stack):

```typescript
// alchemy.run.ts
import * as Fly from "alchemy/Fly";

export const Website = Fly.Website.ReactRouter("Website");
```

Pass `app` to put the site on an existing `Fly.App`. `rootDir` is the
project root — the directory holding `package.json`, `vite.config.*`,
and `react-router.config.ts`. It defaults to `"."`, so set it only when
the app does not sit next to `alchemy.run.ts`:

```typescript
export const Website = Fly.Website.ReactRouter("Website", {
  rootDir: "./web",
});
```

## Add it to the Stack

Yield the site from your Stack and return its URL:

```typescript
// alchemy.run.ts
import * as Alchemy from "alchemy";
import * as Effect from "effect/Effect";

export default Alchemy.Stack(
  "MyReactRouterSite",
  {
    providers: Fly.providers(),
    state: Alchemy.localState(),
  },
  Effect.gen(function* () {
    const site = yield* Website;
    return { url: site.url };
  }),
);
```

Requests matching a built client asset are served first; everything
else — SSR routes, loaders, actions, resource routes — falls through to
the Node handler on the Machine.

The live `url` is `https://{app}.fly.dev`. `site.app`,
`site.service`, `site.ip`, and `site.certificate` are the Fly
resources underneath — `undefined` during `alchemy dev`.

See the [React Router API reference](/providers/fly/website/reactrouter)
for every prop and attribute.

## How the two build outputs are deployed

`react-router build` writes two directories, and both land in the
Machine image:

```
build/
├── client/    -> baked into the image, served first
└── server/    -> wrapped as a Node HTTP server on port 3000
```

`build/client` holds the hashed browser bundles plus everything you put
in `public/`. `build/server/index.js` is a `ServerBuild` manifest, not a
request handler, so Alchemy wraps it with `createRequestHandler` during
the build and emits a Node serve entry — that process is the
[`Fly.Service`](/fly/compute/services).

If you set Vite's `base`, set React Router's `basename` to the same
prefix: the client directory maps onto the site root one-for-one, so the
paths the SSR document emits have to be the paths the image was given.

## Add environment variables

Process environment is a top-level `env` map — plain strings, or
`Redacted` values:

```diff lang="typescript"
// alchemy.run.ts
export const Website = Fly.Website.ReactRouter("Website", {
+  env: {
+    API_BASE: "https://api.example.com",
+  },
});
```

The values are copied onto `process.env` before the build and the
dev server, and onto the Machine at deploy time, so server code
reads the same keys in both modes. Client inlining is whatever Vite
does (`VITE_` prefixes).

An output from another resource only exists once that resource is
yielded, so declare the site inside the Stack generator instead:

```typescript
// alchemy.run.ts
Effect.gen(function* () {
  const api = yield* Api;

  const site = yield* Fly.Website.ReactRouter("Website", {
    env: { API_BASE: api.url },
  });

  return { url: site.url };
});
```

## Read the environment in a loader

The React Router server is a plain Node process. Loaders and
actions read the environment from `process.env`:

```typescript
// app/routes/home.tsx
export function loader() {
  return { apiBase: process.env.API_BASE ?? "unset" };
}
```

Values prefixed `VITE_` are inlined into the client bundle at build time
instead, as `import.meta.env.VITE_*` — set those in the shell that runs
`alchemy deploy`, not in `env`.

## Read the environment in a resource route

A route module with no default export is a resource route, served by the
same Node process:

```typescript
// app/routes/api.hello.ts
export function loader() {
  return new Response(process.env.API_BASE ?? "unset");
}
```

## Build configuration

The build runs through your project's own `vite.config.ts` and
`react-router.config.ts`. A custom `buildDirectory` just works — the
integration observes the directory from the resolved Vite config, so
there is nothing to mirror on the resource.

## Local dev

```sh
bun alchemy dev
```

`alchemy dev` runs React Router's own Vite dev server (native HMR)
instead of deploying; `site.url` is the local address and no Fly
resources are created. Wrap the site in `Alchemy.remote()` to deploy the
live App and Service even during dev:

```typescript
export const Website = Fly.Website.ReactRouter("Website").pipe(
  Alchemy.remote(),
);
```

## React Server Components

RSC is not supported yet. React Router's `unstable` RSC plugin — and
the `future.v8_viteEnvironmentApi` flag — emit more than one server
environment, and this resource assembles a single Node entry. A build
that produces no single server entry fails with an actionable error
rather than deploying something broken.

## Custom domain

```typescript
const site = yield* Fly.Website.ReactRouter("Web", {
  domain: "app.example.com",
});
```

`domain` requests ACME (`Fly.Certificate`) on the App. `url`
becomes `https://app.example.com`. Point DNS at the App first — v1
does not create DNS records.

## Where next

- [React Router API reference](/providers/fly/website/reactrouter)
- [Apps](/fly/compute/apps) and [Services](/fly/compute/services)
- [IPs & certificates](/fly/networking)

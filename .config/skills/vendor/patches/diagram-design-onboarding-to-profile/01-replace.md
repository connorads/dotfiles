{{marker}}

Write the new tokens to `~/.diagram-design/profiles/<slug>.md` per [`profiles.md`](profiles.md), then write a `<project-root>/.diagram-design` marker naming that slug. **Do not write the installed `style-guide.md`**: this install is a vendored copy under version control that `skills update` refreshes in place. Rebuild one example to verify the new skin reads cleanly.

{
  "reason": "upstream's `feedback --search-miss` sends the user's catalog query with no opt-out caveat; gate it the way the hyperframes-cli skill gates its render-rating sibling.",
  "files": [
    ".agents/skills/hyperframes-registry/SKILL.md"
  ]
}

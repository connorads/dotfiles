{{marker}}

**Launcher unavailable:** Read existing PRODUCT.md and DESIGN.md directly, without inventing missing context, then continue with the applicable design references and available tools. Do not install an engine or retry engine-dependent steps. Replace engine-driven context, direction-selection, font and comp helpers with direct inspection and the runtime's existing tools; say which automated checks did not run. Detector, hook administration, live-browser and shortcut commands require a separately provisioned engine: report that requirement instead of claiming they ran. Keep cwd at the user's project and resolve bundled files from this skill's actual base directory.

The visual-polishing budget does not replace required repository checks or permit known functional failures to be left unresolved. Close disposable browser sessions after verification; never close unrelated sessions.

import { defaultConfig } from '../config'

/** Default row 1 buttons: modifiers, navigation, interrupt */
export const defaultRow1 = defaultConfig.toolbar.row1

/** Default row 2 buttons: tmux window nav + paste + drawer */
export const defaultRow2 = defaultConfig.toolbar.row2

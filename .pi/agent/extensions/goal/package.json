{
  "name": "pi-goal",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "A persistent, self-driving objective that steers the agent across turns, drives it toward completion within a token budget, and survives context compaction",
  "pi": {
    "extensions": ["./index.ts"]
  },
  "scripts": {
    "test": "node --test core.test.ts index.test.ts"
  }
}

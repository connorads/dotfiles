{
  "name": "pi-extensions-tooling",
  "version": "0.1.0",
  "private": true,
  "description": "Tooling for the single-file/small pi extensions (atuin, agent-state, web-search): their typecheck project, and the test script for those without a package.json of their own. Deliberately has NO \"pi\" field so the pi host never registers it as an extension; runtime module resolution stays with the host.",
  "scripts": {
    "test": "node --test web-search/core.test.mjs",
    "typecheck": "tsc -p tsconfig.json"
  },
  "devDependencies": {
    "@earendil-works/pi-ai": "0.81.1",
    "@earendil-works/pi-coding-agent": "0.81.1",
    "@types/node": "^26.0.0"
  }
}

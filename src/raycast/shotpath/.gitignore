# Raycast extension build/output + deps. The dotfiles ~/.gitignore mirrors these
# re-ignores; this local file keeps the project self-contained.
node_modules
dist
*.js.map
*.tsbuildinfo

# Generated by `ray develop` / `ray build`
raycast-env.d.ts
.raycast
